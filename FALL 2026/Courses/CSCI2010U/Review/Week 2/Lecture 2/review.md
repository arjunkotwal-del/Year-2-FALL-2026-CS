# Review — Week 2, Lecture 2

## What to review
- [ ] **Translate pseudocode → Java from memory**: write out `Bubblesort` and `Swap` without looking, then compare to [notes.md](../../../Week%202/Lecture%202/notes.md).
- [ ] **Why pass-by-value still lets `Bubblesort` mutate the array**: explain reference types vs. primitives in your own words — this is a classic exam trip-up.
- [ ] **`main` method signature**: be able to explain what each keyword does (`public`, `static`, `void`, `String[] args`) without prompting.
- [ ] **`Arrays.toString()` vs default `toString()`**: know why printing an array directly gives `[I@...` garbage.
- [ ] **Descending-order class question**: confirm you can flip `Bubblesort` to sort descending (`<` → `>`) and explain why that one-line change works.
- [ ] **Efficiency timing data**: understand why n×10 → time×100 hints at O(n²) — don't need Big-O formally yet (that's Lecture 8), but hold onto the intuition.
- [ ] **Take-home coding challenge** (not done live): convert the outer `for` to a `while`, and add the early-exit-on-no-swap optimization.

## Status
Not yet reviewed.
